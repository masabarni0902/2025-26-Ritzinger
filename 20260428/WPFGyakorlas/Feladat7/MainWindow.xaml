<Window x:Class="Feladat7.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Feladat7"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Celsius (°C):"/>
        <Label Grid.Row="0" Grid.Column="1" Content="Fahrenheit (°F):"/>

        <TextBox Grid.Row="1" Grid.Column="0"
             x:Name="txtCelsius"
             Text="0"
             TextChanged="Celsius_TextChanged"
             Margin="0,0,8,0"/>
        <TextBox Grid.Row="1" Grid.Column="1"
             x:Name="txtFahrenheit"
             IsReadOnly="True"
             Background="#F5F5F5"/>

        <Slider Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
            x:Name="slHofok"
            Minimum="-50" Maximum="150"
            Value="0"
            Margin="0,8,0,4"
            ValueChanged="Slider_ValueChanged"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
               x:Name="txtLeiras"
               HorizontalAlignment="Center"
               Text="Normál szobahőmérséklet"/>
    </Grid>

</Window>
